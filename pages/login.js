import Layout from '../components/layout';

const Login = () => (
    <Layout>
        <div>
            <h1>Login</h1>
            <h4>Login to where?</h4>
        </div>
    </Layout>
);

export default Login;